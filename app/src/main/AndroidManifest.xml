<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.ualberta.CMPUT3012019T02.alexandria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ViewUserProfileActivity"></activity>
        <activity android:name=".activity.BlockedUsersActivity" />
        <activity android:name=".activity.EditMyProfileActivity" />
        <activity android:name=".activity.MyProfileActivity" />
        <activity android:name=".activity.viewuserbook.ViewBorrowedUserBookActivity" />
        <activity android:name=".activity.viewuserbook.ViewRequestedUserBookActivity" />
        <activity android:name=".activity.viewuserbook.ViewAcceptedUserBookActivity" />
        <activity android:name=".activity.viewuserbook.ViewAvailableUserBookActivity" />
        <activity android:name=".activity.SignUpActivity"/>
        <activity
            android:name=".activity.MainActivity"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name" />
    </application>

</manifest>